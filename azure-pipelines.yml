# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
- dev
- azure-pipelines

pool:
  name: win64-notebook

steps:
- script: |
    generate_projects.bat
  displayName: 'Generate Thirdparty Projects'
