## Build

- When building the project, always build only one project at a time. All dependencies will be built automatically. In most cases, building the `Engine` project is sufficient; if a `Game` project exists, build the `Game` project instead.
- Do not build C# projects independently, except for the purpose of checking compilation errors.
